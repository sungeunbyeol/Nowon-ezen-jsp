<%@ page language="java" contentType="text/html; charset=EUC-KR"
    pageEncoding="EUC-KR"%>
    
    
<% 
	//1 넘어온 데이터를 받기
	request.setCharacterEncoding("EUC-KR"); //post방식에서의 한글처리, get방식의 한글처리는 제공하지 않는다.
%>
<jsp:useBean id="bdao" class="my.book.BookDAO"/>
<jsp:setProperty property="*" name="bdao" />

<%
	//2 데이터 유효성 검사
	if(bdao.getBookname()==null || bdao.getPublisher()==null ||bdao.getWriter()==null || bdao.getPrice()==null ||
	bdao.getBookname().trim().equals("")
	|| bdao.getPublisher().trim().equals("") || bdao.getWriter().trim().equals("")){%>
		<script type="text/javascript"> 
			alert("데이터를 모두 입력하셔야 합니다.")
			history.back()								  
		</script>
<% return;
	}
